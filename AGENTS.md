- 后端端口和数据库请给一个不常用的值，防止冲突
- 项目为pnpm monopore + ts的技术栈，避免出现any，允许使用unknown
- 前端为react + Compiler，后端为hono
- prd等文档在/docs目录下，有改动和变动请同步，此外在开始行动前也应当先查看文档
- 注释使用中文，重点模块需要包含注释
- 测试使用vitest这个框架，重要模块确保都包含测试用例，测试文件放到当前文件夹的test目录下
- 请求工具用hono的hc，避免其他请求库
- 修改代码完成后请运行eslint和ts校验，必要时候还要运行代码测试
- 每次需要动手写代码之前先扫描看下package.json的依赖，如果有成熟解决方案直接复用，没有的话则考虑有无社区方案，最后才是自己实现
- 代码行数不要超出400行，超出考虑切分，特殊情况除外
- 测试数据库注意环境隔离，不要污染生产环境
- pnpm尊重本机的pnpm版本
